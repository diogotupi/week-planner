# Só necessário para deploy local com `vercel dev`
# No Vercel, Neon e AUTH_SECRET são configurados no dashboard

DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
AUTH_SECRET=coloque-uma-string-secreta-longa-aqui
